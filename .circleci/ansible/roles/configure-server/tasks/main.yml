---
- name: Install nvm
  shell: "curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash"
  args:
    creates: "{{ ansible_env.HOME }}/.nvm/nvm.sh"

- name: Install node and set version
  become: true
  become_user: root
  shell: "source ~/.nvm/nvm.sh && nvm install 13.18.0 && nvm alias default 13.18.0"
  args:
    executable: /bin/bash
    creates: "{{ ansible_env.HOME }}/.nvm/alias"

- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present
